<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Состояние сети — @capacitor/network: показать «нет интернета»
         вместо молчаливой ошибки, когда аят не скачан и стрим недоступен. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Фоновое воспроизведение.
         Что известно точно (сверено с исходниками @capacitor/android):
         Bridge.onPause() НЕ приостанавливает WebView — он только
         оповещает плагины, а webView.onPause()/pauseTimers() не
         вызывает.  Значит <audio> в фоне не глушится самим Capacitor,
         и navigator.mediaSession (он у нас настроен в lib/mediaSession.ts)
         должен поднять системное медиа-уведомление силами WebView.
         ⚠️ НЕ ПРОВЕРЕНО на устройстве: Android вправе убить фоновый
         процесс без foreground service, поэтому длительное чтение с
         погашенным экраном может обрываться.  Если проверка это
         подтвердит — понадобится нативный foreground service с
         MediaSession; разрешения под него объявлены здесь заранее,
         чтобы не пересобирать манифест и не ловить сюрприз на ревью.
         На iOS сценарий закрыт ключом UIBackgroundModes=audio. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Android 13+ требует явного разрешения, чтобы показать
         уведомление-плеер (и любые будущие напоминания о намазе). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
