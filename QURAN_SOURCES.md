# Реестр кораничных источников

Требование `CLAUDE.md` (сакральные правила, §9): всё, что цитирует Коран,
должно быть перепроверяемо одним проходом — с указанием издания, откуда
взят текст, и даты получения.

## Основной источник текста

Весь кораничный текст приложения берётся из одного места —
`src/content/quran-sources.ts`. Это машинный реестр на **6236 аятов**, по
одной записи на аят:

| Поле | Что в нём |
|---|---|
| `arabic` | арабский, edition `ar-simple-clean` |
| `translations.ru` | перевод, edition `ru-kuliev` (Эльмир Кулиев) |
| `editions` | какие именно издания использованы |
| `fetchedAt` | дата получения записи |

Русский перевод в проекте один — **Кулиев**. Другие русские переводы не
используются нигде: единый голос важнее разнообразия, иначе читатель
встречает разную стилистику в разных углах приложения.

## Реестр прав перед App Store

Доступность файла в интернете и бесплатность приложения не равны лицензии на
перераспространение. Перед `Submit for Review` каждый статус `Нужно подтвердить`
должен стать `Подтверждено` со ссылкой и сохранённой копией разрешения либо
материал должен быть заменён.

| Материал | Источник | Что уже известно | Статус для релиза |
|---|---|---|---|
| Арабский текст `quran-uthmani` | `api.alquran.cloud` / сверка Quran.com | Источник и дата получения записаны; нужна точная лицензия набора данных | **Нужно подтвердить** |
| Перевод Эльмира Кулиева `ru.kuliev` | `api.alquran.cloud` | Бесплатный API не доказывает право на публикацию самого перевода | **Нужно подтвердить** |
| QCF4 JSON/schema | `public/qcf4/LICENSE.md` | Явная лицензия MIT | **Подтверждено** |
| Разметка страниц QCF V1 — заголовки сур и басмала | дамп `sql/` репозитория `quran/quran.com-images` | Извлечено 226 строк разметки на 116 страниц (`scripts/gen/data/qcf1-decor-lines.json`). README репозитория говорит дословно: «The code is copyleft GPL (read: free) but the actual fonts and pages (in the `res/fonts` directory) belong to the King Fahd Quran Complex in Saudia Arabia». Дамп лежит в `sql/`, то есть вне названного каталога, но формулировка неточная и приводится как есть, без толкования | **Нужно подтвердить** |
| QCF4/QCF1/QPC V4 Tajweed font files | государственный King Fahd Complex / производные наборы | Не public domain: официальная платформа предоставляет их безвозмездно для мобильных, браузерных и настольных коранических приложений; владелец подтвердил использование строго для Корана | **Подтверждено**, основания — `docs/licenses/ARABIC_QURAN_FONTS.md` |
| Noto Naskh и Inter | локальные файлы Google Fonts | OFL; сохранить тексты OFL рядом с файлами | **Нужно собрать копии лицензий в одном месте** |
| Аудио Аляфаси, Аш-Шатри, Ад-Даусари и Аль-Аджми | QuranAudio/QuranCDN (`download.quranicaudio.com`) онлайн; `cdn.islamic.network`, `everyayah.com` для поаятного офлайна | Онлайн — 114 непрерывных MP3 по одной суре для чистых переходов; офлайн — по одному MP3 на аят. Потоки доступны публично, точная лицензия на встраивание/офлайн-копию не сохранена | **Нужно подтвердить** |
| Аудио Мухаммада Аль-Люхайдана | официальный каталог `mp3quran.net`, `server8.mp3quran.net/lhdan/` | Полный Коран, 114 MP3 по одной суре; `scripts/gen/split-luhaidan-ayahs.mjs` воспроизводимо нарезает их по проверенным границам в 6236 коротких серверных файлов. Они не входят в пакет приложения | **Нужно подтвердить право на перераспределение производных MP3** |
| Пословные тайминги Аляфаси, Аш-Шатри и Ад-Даусари | Quran.com / QuranCDN | По 6236 записей у Аляфаси и Ад-Даусари; у Аш-Шатри 6233 непустых набора и 3 аята честно работают без маркера; условия перераспределения не сохранены | **Нужно подтвердить** |
| Границы аятов Аль-Люхайдана | `m7mdiyat-tafsir-data/timings/luhaidan/` | 6236 границ forced alignment; детальные файлы называют те же MP3Quran-записи и совпадают с ними по длительности; компактная копия генерируется в `src/content/luhaidan-ayah-ranges.ts` | **Нужно подтвердить разрешение на перераспределение таймингов** |
| Границы аятов Аляфаси, Аш-Шатри, Ад-Даусари и Аль-Аджми | Quran.com/QuranCDN `audio_files.verse_timings` | 24 944 проверенные границы соответствуют тем же непрерывным QuranAudio-записям; локальная копия генерируется в `src/content/qurancom-ayah-ranges.ts`, чтобы перед первым звуком не было API-запроса | **Нужно подтвердить условия перераспределения таймингов** |

У Аль-Аджми Quran.com отдаёт границы аятов, но поле пословных сегментов пустое,
поэтому интерфейс честно не обещает пословную подсветку. Для его поаятного аудио
используется 128 kbps каталог `Ahmed_ibn_Ali_al-Ajamy_128kbps_ketaballah.net`:
64 kbps вариант содержит аномально длинные первые файлы нескольких сур и в
приложение намеренно не включён.

Официальные основания для арабских шрифтов и их применения в приложении
зафиксированы в `docs/licenses/ARABIC_QURAN_FONTS.md`. Локальный файл лицензии
конкретной сторонней упаковки `public/qcf4/LICENSE.md` сохраняется вместе с
набором и не удаляется.

## Разметка мусхафа QCF V1 «Мадани 1405»

Постраничный мусхаф V1 показывает страницу 1:1 с печатным изданием, и для
этого нужны две вещи: какое слово на какой строке и чем набраны заголовок
суры с басмалой. Первое отдаёт API, второе — нет, ни при каком значении
параметра `mushaf`.

| Что | Откуда | Дата |
|---|---|---|
| Слова аятов, разбиение по строкам | `api.qurancdn.com/api/qdc/verses/by_page/{N}?mushaf=2` | 2026-08-31 |
| Строки заголовков сур и басмалы | `github.com/quran/quran.com-images`, `sql/02-database.sql` + патч `sql/03-basmallah-shaddah.sql` | 2026-08-31 |
| Начертания | `public/qcf1/fonts-woff2/` — уже в проекте, новых файлов не добавлялось | ранее |

Второй источник — оригинальный генератор постраничных картинок V1, которым
пользовались quran.com и его мобильные приложения. В нём лежит тот же самый
шрифт `QCF_BSML` и таблицы `glyph` / `glyph_page_line` с типом строки
`sura` / `ayah` / `bismillah`.

### Что проверено перед тем, как это попало в приложение

Сакральный текст не принимается на веру ни от источника, ни от агента.
Прогонялось вживую:

- Разметка сплошная: 604 страницы, строки без дыр, по 15 на странице и по 8
  на первых двух.
- **114 строк заголовков и ровно 112 строк басмалы.** Разница в две штуки
  осмысленна и служит проверкой сама по себе: у Аль-Фатихи басмала — это
  первый аят, у Ат-Тауба басмалы нет вовсе.
- Все 125 использованных кодов присутствуют в нашем файле шрифта и реально
  прорисованы.
- Отрисовка прочитана глазами: سُورَةُ الفَاتِحَةِ, سُورَةُ المَائِدَةِ,
  سُورَةُ التِّينِ, سُورَةُ النَّاسِ и все три варианта басмалы. Заголовок
  суры 5 совпал с тем, что независимо лежит в данных V4.
- Сверка с живым API на страницах 1, 2, 50, 106, 187, 208, 221, 255, 300,
  597, 598, 604: множество строк-аятов из дампа совпало с ответом API.
  Расхождений 0.

### Две ловушки, на которых легко ошибиться молча

**Коды заголовков нельзя вычислять формулой.** `0xFB8C + N` даёт верный код
только для 37 сур из 114: на суре 38 в шрифте разрыв (U+FBB1 → U+FBD3).
Поэтому в репозитории лежит таблица, а не арифметика.

**Пробел в нумерации строк не говорит, что рисовать.** На странице 187
(сура 9) и на странице 208 пропущено по одной строке, но на первой это
заголовок, а на второй — басмала, потому что заголовок суры 10 остался на
предыдущей странице. Достраивание разметки «по дыркам» дало бы неверную
страницу Корана, и ни типы, ни сборка этого не поймали бы.

Отдельно про сам шрифт `QCF_BSML`: обычные арабские коды U+06xx в нём ведут
на пустую прямоугольную заглушку, хотя в `cmap` они присутствуют. Проверка
«есть ли символ в таблице кодировки» здесь врёт — сверять надо контур глифа.

## Где этот текст показывается

| Экран | Что берёт |
|---|---|
| Чтение суры (лента аятов) | арабский глифами QCF V4 + перевод Кулиева |
| Книжный вид (страница мусхафа) | арабский глифами QCF V4, перевод по тапу |
| Поиск | перевод Кулиева, все 6236 аятов |
| Раздел «Дуа» | фрагменты аятов — см. ниже |
| Азкары | собственные тексты владельца, не из этого реестра |

Глифы мусхафа (QCF V4, King Fahd Complex, мединский мусхаф 1441 г. х.)
описаны отдельно — `MUSHAF_FONTS.md`.

## Дуа из Корана — 30 записей

`public/dua/dua.json`, категория `quran`.

**Список, порядок, ссылки и транскрипция** — из файла владельца
«30 дуа из Корана в месяц Рамадан.pdf» (`~/Desktop/QuranIng`).

**Арабский и перевод** — не из PDF, а из реестра выше. Причины:

- при извлечении из PDF арабский рассыпается: лигатура «Аллах» распадается
  на отдельные буквы, выравнивание по ширине набивает растяжки-тatweel
  внутрь слов;
- в переводе PDF есть опечатки («защит» вместо «защити»);
- перевод Кулиева и так зафиксирован для проекта, и текст на карточке дуа
  обязан совпадать с тем, что человек прочитает в самом Коране приложения.

**Границы фрагмента** сняты со страниц PDF глазами и применены к тексту из
реестра: скрипт находит начало и конец по нормализованному написанию и
возвращает срез исходной строки, поэтому огласовки, знаки паузы и васлы
остаются те же байты, что в издании.

Почти все эти дуа — **фрагмент аята**, а не аят целиком: например, записи
№3 и №4 — две разные мольбы внутри 2:286. Правила проекта это допускают при
явной атрибуции, поэтому ссылка `Коран сура:аят` стоит на карточке открыто,
рядом с текстом, а не под кнопкой.

### Что процитировано

| № | Ссылка | № | Ссылка | № | Ссылка |
|---|---|---|---|---|---|
| 1 | 2:127 | 11 | 7:126 | 21 | 21:83 |
| 2 | 2:201 | 12 | 7:155–156 | 22 | 23:97 |
| 3 | 2:286 | 13 | 11:47 | 23 | 23:109 |
| 4 | 2:286 | 14 | 12:101 | 24 | 25:65 |
| 5 | 3:8 | 15 | 14:40 | 25 | 25:74 |
| 6 | 3:147 | 16 | 14:41 | 26 | 26:83–85 |
| 7 | 3:193 | 17 | 17:24 | 27 | 27:19 |
| 8 | 5:114 | 18 | 18:10 | 28 | 59:10 |
| 9 | 7:23 | 19 | 20:25–28 | 29 | 66:11 |
| 10 | 7:89 | 20 | 20:114 | 30 | 71:28 |

Дата сборки: 2026-08-12.

### Одно осознанное отступление

У Кулиева мольба стоит внутри прямой речи: `«Господь наш! …»`. Вырезанный
фрагмент забирал открывающую кавычку без закрывающей, и на карточке висела
одинокая `«`. Непарная кавычка снимается — это обрамление речи, а не слова
мольбы; сами слова не меняются.

## Как перепроверить

```bash
npm run check:sacred
```

Сверяет пять контрольных аятов с сохранёнными снимками — ловит случайную
правку текста в данных. Полная сверка реестра с внешним источником делается
вручную по `editions` и `fetchedAt`.
