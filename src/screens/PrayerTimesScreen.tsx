/**
 * PrayerTimesScreen — раздел «Намаз».  Пока заглушка.
 *
 * Что здесь появится (для следующей сессии, чтобы не выдумывать
 * заново): расчёт времён по координатам устройства, выбор метода
 * расчёта (Умм-аль-Кура / Египетская организация / Мусульманская
 * лига и т.д.) и мазхаба для асра, ближайший намаз с обратным
 * отсчётом, напоминания.
 *
 * Расчёт делать локально по формулам, а не запросом к API: времена
 * намаза нужны офлайн, а зависимость от чужого сервиса в такой
 * функции — плохая идея.
 */

import { Clock } from '../components/icons';
import { ComingSoonScreen } from './ComingSoonScreen';

export function PrayerTimesScreen() {
  return (
    <ComingSoonScreen
      title="Намаз"
      icon={<Clock size={26} />}
      lead="Времена намаза по вашему городу, ближайший намаз и напоминания."
      points={[
        'Расчёт по координатам, работает без интернета',
        'Выбор метода расчёта и мазхаба для асра',
        'Обратный отсчёт до следующего намаза',
        'Напоминания перед началом времени',
      ]}
    />
  );
}
