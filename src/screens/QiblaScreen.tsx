/**
 * QiblaScreen — раздел «Кибла».  Пока заглушка.
 *
 * Что здесь появится (для следующей сессии): азимут на Каабу от
 * текущих координат по формуле большого круга и стрелка-компас,
 * поворачивающаяся по данным магнитометра.
 *
 * Две вещи, которые обязательно всплывут при реализации:
 *  • На iOS доступ к компасу требует явного разрешения через
 *    DeviceOrientationEvent.requestPermission(), и запрашивать его
 *    можно только из обработчика пользовательского жеста.
 *  • Магнитный север не равен истинному.  Нужна поправка на
 *    магнитное склонение, иначе стрелка врёт на единицы градусов —
 *    для киблы это заметно.
 */

import { Compass } from '../components/icons';
import { ComingSoonScreen } from './ComingSoonScreen';

export function QiblaScreen() {
  return (
    <ComingSoonScreen
      title="Кибла"
      icon={<Compass size={26} />}
      lead="Направление на Каабу от вашего местоположения."
      points={[
        'Стрелка-компас по данным магнитометра',
        'Точный азимут в градусах и расстояние до Мекки',
        'Ручной ввод города, если геолокация недоступна',
      ]}
    />
  );
}
